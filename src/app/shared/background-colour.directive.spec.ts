import { BackgroundColourDirective } from './background-colour.directive';

describe('BackgroundColourDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundColourDirective();
    expect(directive).toBeTruthy();
  });
});
